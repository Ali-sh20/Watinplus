import 'package:flutter/material.dart';
import 'package:hexcolor/hexcolor.dart';

class drvier_homeScreen extends StatelessWidget {
  const drvier_homeScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
        thickness: .5, indent: 0, endIndent: 0, color: HexColor('#707070'));
  }
}
